# MBTI 검증 딥리서치 에이전트 — 프로젝트 계획서

> **한 줄 요약** — "MBTI 궁합론은 근거가 있는가"를 영문 위키백과 52건에서 스스로 조사해 한국어 보고서로 써 오는 멀티 에이전트를 만들고, 그 협업 과정을 아이소메트릭 연구소에서 캐릭터가 움직이는 모습으로 실시간 시각화해 배포한다.

작성일: 2026-09-22 · 기반: 모두의연구소 8강 「딥리서치 에이전트 만들기 — 오케스트레이션」

---

## 1. 개요

### 1.1 만드는 것

| 층 | 산출물 |
|---|---|
| **파이프라인** | LangGraph 6노드 — 코디네이터 1명이 조사관 4명을 병렬 파견하는 딥리서처 |
| **시각화** | 의존성 0 · Canvas 아이소메트릭 연구소. 조사관이 서고를 오가며 일하는 모습 |
| **배포** | Vercel. 기본은 녹화 재생(키 불필요), 옵션은 방문자 본인 키로 라이브 실행 |

### 1.2 왜 이 주제인가

MBTI 궁합론은 **대중적으로 널리 믿기지만 학술적 근거가 빈약한** 주제다. 이 파이프라인의 핵심 장치가 **"문장마다 «문서명» 근거 붙이기"**이므로, 주제 자체가 그 장치의 의미를 증폭시킨다.

> 질문: **"MBTI 궁합론은 어떤 근거로 제시되며, 심리학계는 이를 어떻게 평가하는가?"**

보고서는 한국어로 쓰되 코퍼스는 영문이다. 원문 언어 ≠ 산출물 언어라는 점도 이 구조가 감당할 수 있음을 보여주는 사례가 된다.

---

## 2. 주제와 코퍼스 — 실측 근거

### 2.1 코퍼스가 통과해야 할 4조건 (강의 근거)

| 조건 | 왜 필요한가 | 어기면 |
|---|---|---|
| ① 답이 여러 문서에 흩어져 있을 것 | 한 문서로 끝나면 나눌 일이 없다 | 조사관 넷 중 셋이 헛읽는다 (16강 Q1) |
| ② 문서가 길 것 | 짧으면 컨텍스트 격리가 무의미 | 이 구조를 쓸 이유 자체가 사라진다 |
| ③ 문서끼리 링크로 이어질 것 | 프런티어 탐색의 생명줄 | "읽은 내용이 다음 선택을 만든다"가 죽는다 (9강) |
| ④ 절이 자연스럽게 나뉠 것 | 나눌 것이 실제로 나뉘어 있어야 한다 | 넷이 같은 문서를 네 번 읽는다 (5강) |

### 2.2 후보 3안 실측 비교

| | 한국어 MBTI | 한국어 심리학 | **영문 심리학 (채택)** | 원본(근대사) |
|---|---|---|---|---|
| 문서 수 | 10건 | 19건 | **52건** | 34건 |
| 총 본문 글자 | 199,369 | 587,861 | **1,705,434** | 447,487 |
| 평균 길이 | 19,936 | 30,940 | **32,797** | 13,161 |
| 내부 링크 | — | 48개 | **485개** | 428개 |
| 문서당 링크 | — | 2.5 | **9.3** | 12.6 |
| 고립 문서 | — | 1건 | **1건** | 1건 |
| 판정 | ❌ 16유형이 전부 리다이렉트 | ❌ 링크가 원본의 1/5 | **✅ 전 조건 통과** | 기준 |

**탈락 사유 기록**

- **한국어 MBTI**: 16개 유형(INFP…ESTJ) 문서가 **전부 「MBTI」 하나로 리다이렉트**. 조사관 넷이 같은 문서 하나를 읽게 되어 조건 ①③이 동시에 무너진다.
- **한국어 심리학**: 분량(587K)은 충분하나 `빅 파이브`(1K)·`한스 아이젠크`(1K)·`신뢰도`(2K)·`심리측정학`(4K) 등 **비판의 핵심 근거가 토막글**. 링크 밀도 2.5는 프런티어가 두세 번에 막힌다.

### 2.3 채택 코퍼스 상세

```
출처   en.wikipedia.org
문서   52건 · 1,705,434자 · 평균 32,797자
링크   485개 (문서당 9.3)
토큰   약 852,700 — gpt-4o-mini 창(128,000)의 6.7배
```

**컨텍스트 창의 6.7배**라는 점이 중요하다. 원본 근대사 코퍼스는 1.7배였으므로, "혼자서는 물리적으로 불가능하다"는 전제가 원본보다 4배 선명하다.

### 2.4 서고(분류) 구성

| 서고 | 건수 | 대표 문서 |
|---|---|---|
| 측정 | 11건 | Psychometrics, Reliability, Replication crisis(101K), Factor analysis(77K) |
| 유형론 | 11건 | Carl Jung(96K), MBTI(37K), Enneagram, Socionics, Psychological Types |
| 성격심리 | 11건 | Big Five(91K), Trait theory, Hans Eysenck, Raymond Cattell, 16PF |
| 유사사례 | 7건 | Pseudoscience(47K), Astrology, Blood type personality theory, Phrenology |
| 편향 | 6건 | Barnum effect, Confirmation bias(58K), Self-fulfilling prophecy |
| 관계 | 6건 | Interpersonal attraction, Assortative mating, Social psychology |

이 분류는 시각화의 **서고 배치와 1:1로 대응**한다. 조사관이 어느 서고로 걸어가는지가 곧 배정 로그가 된다.

### 2.5 고립 문서 — 버그가 아니라 자산

`Assortative mating`(유유상종 짝짓기)은 **어떤 문서도 링크하지 않는다.** 원본의 «거문도 사건»과 정확히 같은 위치다.

그런데 이 문서는 주제상 **"사람들이 실제로 비슷한 상대와 맺어지는가"**라는, 궁합론 검증의 핵심 근거다. 즉:

> **링크로는 절대 닿을 수 없는데, 질문에는 꼭 필요한 문서.**

4강에서 고른 "카드(제목 + 앞 250자)" 결정이 값을 하는지를 검증할 자리가 공짜로 생겼다. 카드를 끄면 이 문서에 아무도 닿지 못하고, 그 사실이 보고서에서 드러나야 한다.

---

## 3. 아키텍처

### 3.1 파이프라인 6노드

```
질문 ─▶ ① 기획 ─▶ ② 배치 ─┬▶ ③ 조사관 A ─┐
       목차·역할·배정   Send  ├▶ ③ 조사관 B ─┤
                       팬아웃 ├▶ ③ 조사관 C ─┼▶ ④ 점검 ─done─▶ ⑤ 종합 ─▶ ⑥ 평가 ─▶ 보고서+지표
                             └▶ ③ 조사관 D ─┘   빈 칸 찾기        절 이어붙이기
                                    ▲                 │
                                    └────── more ─────┘
```

### 3.2 이벤트 스트림 분리 (핵심 원칙)

`agent-office` 프로젝트의 설계 원칙을 **개념만** 가져온다. 코드는 새로 짠다.

```
 [이벤트 소스]          [번역/연출]         [월드 상태]        [렌더러]
 무슨 일이 있었나   →   어떻게 보여줄까   →   지금 상태     →   화면
 ──────────────      ─────────────      ──────────      ────────
 재생: runs/*.json    Choreographer      World            Canvas
 라이브: SSE          (프런트)           (agents/sections) (아이소메트릭)
```

**백엔드는 "조사관이 어느 서고로 걸어가야 하는지" 몰라도 된다.** 그 번역은 전적으로 프런트 Choreographer의 몫. 이 경계가 흐려지면 나중에 절제 실험 스위치를 넣을 때 백엔드를 다시 뜯게 된다.

### 3.3 두 가지 실행 모드

| | 재생 모드 (기본) | 라이브 모드 (옵션) |
|---|---|---|
| 이벤트 소스 | `runs/*.json` 정적 파일 | SSE 스트림 |
| API 키 | 불필요 | 방문자 본인 키 |
| 백엔드 | 없음 (정적 배포) | Python 함수 |
| 대기 시간 | 즉시 | 30~40초 |
| 목적 | 누구나 즉시 관람 | 자기 질문으로 실행 |

16강이 `결과자료.json`으로 "API 키가 없어도 읽을 수 있습니다"를 한 것과 같은 발상이다.

---

## 4. 이벤트 프로토콜 (프런트/백 사이의 유일한 계약)

모든 이벤트는 `{ v, type, ts, ...payload }` 형태.

### 4.1 의미 이벤트 (백엔드 → 프런트)

```jsonc
{ "v":1, "type":"run.start",        "question":"MBTI 궁합론은…", "설정":{"절수":4,"절예산":3,"배정":true,"구역":true,"역할":true,"재위임":true} }
{ "v":1, "type":"plan.done",        "목차":[{"절":"융의 유형론","역할":"큰그림 담당","시작문서":"Carl Jung","서고":"유형론"}, …] }
{ "v":1, "type":"dispatch",         "바퀴":1, "인원":4, "배치":[0,1,2,3] }
{ "v":1, "type":"researcher.start", "절":"융의 유형론", "역할":"큰그림 담당", "시작문서":"Carl Jung", "피하기":["Psychometrics", …] }
{ "v":1, "type":"researcher.read",  "절":"융의 유형론", "문서":"Carl Jung", "서고":"유형론", "원문자수":96523, "메모자수":412, "관련":true }
{ "v":1, "type":"researcher.done",  "절":"융의 유형론", "자수":631, "인용":9, "충분":true, "부족":"" }
{ "v":1, "type":"researcher.fix",  "절":"융의 유형론", "허위":["16PF Questionnaire"], "남은허위":[], "채택":true }
{ "v":1, "type":"review",           "절수":4, "빈칸":["청일…"], "종료":null }
{ "v":1, "type":"synthesize",       "절수":4, "보고서자수":2796 }
{ "v":1, "type":"evaluate",         "metrics":{ … } }
{ "v":1, "type":"run.end" }
```

`researcher.read`에 **원문자수와 메모자수를 함께** 싣는 것이 핵심이다. 화면에서 "두꺼운 책 → 얇은 메모"로 **컨텍스트 격리가 그대로 보인다**.

`researcher.fix`는 **허위 인용을 잡아 다시 쓰게 한 순간**이다. 12편 중 5편에만 나온다(`채택:false`면 고쳐 온 원고가 더 나쁘라 버렸다는 뜻). **조건부로만 나오는 유일한 이벤트**이므로, 재생기는 이 타입이 없는 녹화도 그대로 재생해야 한다.

### 4.2 번역 규칙 (의미 → 연출)

| 의미 이벤트 | Choreographer 동작 |
|---|---|
| `plan.done` | 소장이 화이트보드로 이동 → 절 카드 4장 부착 → `say:"넷으로 나눕니다"` |
| `dispatch` | 조사관 4명이 **동시에** 각자 배정 서고로 출발 (팬아웃이 눈에 보이는 순간) |
| `researcher.read` | 서고에서 책 꺼내는 모션 → 머리 위에 `원문자수` 두께의 책 → 나올 때 `메모자수` 크기 쪽지로 축소 |
| `researcher.read(관련:false)` | 책을 덮고 `say:"관련 없음"` → 빈손 |
| `researcher.done(인용:0)` | **빨간 테두리** + `say:"근거를 못 찾았어요"` |
| `review(빈칸 있음)` | 해당 조사관에게 빨간 딱지 → 서고로 **되돌아가는** 애니메이션 |
| `evaluate` | 계기판 7개 숫자가 하나씩 채워짐 |

### 4.3 연출 명령 (프런트 내부 생성 — 백엔드는 모름)

```jsonc
{ "type":"move",   "agentId":"r1", "to":[7,3] }
{ "type":"status", "agentId":"r1", "status":"reading" }
{ "type":"say",    "agentId":"r1", "text":"Carl Jung 읽는 중… 📖", "duration":3 }
{ "type":"carry",  "agentId":"r1", "size":"book" | "memo" | "none" }
```

---

## 5. 시각화 설계

### 5.1 연구소 레이아웃

```
┌──────────────────────────────────────────────────────────┐
│  📋 화이트보드(목차)      📚 유형론    📚 성격심리          │
│                                                           │
│  🪑 ① 소장 책상           📚 측정      📚 편향             │
│                                                           │
│  🔍 ④ 검토 테이블         📚 유사사례   📚 관계            │
│                                                           │
│  🖨️ ⑤ 조판대   📊 ⑥ 계기판    🪑🪑🪑🪑 조사관 책상        │
└──────────────────────────────────────────────────────────┘
```

### 5.2 등장인물

| ID | 역할 | 화면에서 하는 일 |
|---|---|---|
| `coord` | 소장 (코디네이터) | 화이트보드에 목차 부착 → 검토 테이블에서 빈 칸 점검 → 조판대에서 머리말 작성 |
| `r1`~`r4` | 조사관 | 배정 서고로 이동 → 책 읽기 → 링크 타고 다음 서고 → 자기 책상에서 원고 집필 |

역할(`큰그림/시간순/인물/원인/비교 담당`)은 캐릭터 색과 이름표로만 구분한다.

> **화면 문구 제약 (14.4 실측)** — 17강은 역할에 효과가 없다고 했고, 우리 12편도 역할을 켜서 좋아졌다는 증거를 주지 못했다. 그러나 각 칸이 1회 실행이라 **"역할은 효과가 없다"고 화면에 단정할 수 없다.** 쓸 수 있는 문장은 "이 실행들에서는 역할을 켠 쪽이 더 낫지 않았다"까지다. 단정하려면 칸당 3회 반복(₩1,100 · 15분)이 먼저다.

### 5.3 시각화가 드러내는 강의 개념

| 강의 개념 | 화면에서 보이는 것 |
|---|---|
| 팬아웃 (Send) | 배치 순간 4명이 **동시에** 흩어진다 |
| 구역 분할 (피하기) | 각자 **다른 서고**로 간다. 끄면 한 서고에 몰린다 |
| 컨텍스트 격리 5% | 두꺼운 책 → 얇은 메모 |
| 재위임 루프 | 빨간 딱지 → **되돌아가는** 애니메이션 |
| 인용 0곳 절 | 빈손으로 돌아온 조사관 |
| 고립 문서 | `Assortative mating` 서가는 **링크로는 닿을 수 없다**. 배정이 직접 보내야만 간다 |
| 허위 인용 교정 | `researcher.fix` — 빨간 딱지를 받고 **원고를 고쳐 쓴다** (12편 중 5편) |

---

### 5.4 에셋 — 전부 Kenney · CC0

사람 캐릭터를 쓰지 않는다. **조립식 도형 캐릭터**를 쓴다.

| 용도 | 팩 | 사용 | 규격 |
|---|---|---|---|
| 캐릭터 | Shape Characters (1.0) | 104 파일 | 80×80px · 몸통+얼굴+손 조립 |
| 바닥·벽·가구 | Furniture Kit (2.0) — **3D 원본을 직접 렌더** | 26종 × 4방향 = 104 파일 | 384×768px · 2:1 정투영 |

합계 **208 파일 · 5.5MB**. 15.1절에 왜 직접 렌더했는지 적었다. 라이선스는 전부 **CC0 1.0** — 상업적 사용 자유, 저작자 표시 의무 없음. 제작자 요청에 따라 `web/assets/CREDITS.md`에 자발적으로 표기한다.

#### 도형 캐릭터를 고른 이유

1. **주제가 성격 유형론 비판이다.** 사람 캐릭터를 쓰면 "이 캐릭터는 무슨 유형처럼 생겼나" 같은 불필요한 해석이 붙는다. 도형은 그 문제가 원천적으로 없다.
2. **부품이 분리돼 있어 상태를 그림으로 말할 수 있다.** 4.2절의 번역 규칙이 그대로 구현된다.
3. **방향 문제를 우회한다.** 2D 아이소메트릭 사람 캐릭터는 CC0로 구하기 어렵다(조사 결과 Kenney·OpenGameArt·itch.io 모두 부적합). 도형은 눈동자 방향으로 시선을 표현해 4방향 스프라이트 없이 해결한다.

#### 부품 → 상태 대응표

| 표정 (얼굴 12종 + 부품 14종) | 의미 |
|---|---|
| 😊 happy | 충분 신고 |
| 😕 sad | 부족 신고 |
| 😠 angry | **인용 0곳 — 경보** |
| 👀 eye_half | 읽는 중 |
| 😎 smirk | 절 완성 |
| 😐 closed | 관련 없음 |

| 손 (6종) | 의미 |
|---|---|
| open | 빈손 — 관련 없음 |
| closed | 메모 소지 |
| point | 서고 지목 |
| thumb | 절 완성 |
| peace | 재위임 복귀 |

| 색 (5종) | 배역 |
|---|---|
| purple + squircle | 소장 (코디네이터) |
| blue / green / pink / red + circle | 조사관 A~D |

#### 서고 상태를 그림으로

`bookcaseBooks`(책 찬 서가) ↔ `bookcaseEmpty`(빈 서가)를 바꿔 끼워 **아직 아무도 안 읽은 서고**를 한눈에 구분한다. 조사관이 읽을수록 서가가 채워진다.

#### 검토했으나 탈락한 것

| 팩 | 사유 |
|---|---|
| Kenney Toon Characters | 측면 뷰 플랫포머용 |
| Kenney Robot Pack | 측면·상단만, 4방향 없음 |
| Kenney Animal Pack | 정면 단일 방향 |
| Kenney Mini/Blocky Characters | 3D 모델 — Blender 렌더링 공정 필요 |
| itch.io 8-Directional Pack | 유료 ($5) |
| OpenGameArt 아이소메트릭 캐릭터 | 판타지 테마 — 컨셉 불일치 |

---

### 5.5 재생 타이밍 — `ts`는 순서만, 시간은 연출이 정한다

**실측** — 다갈래·기본 35개 이벤트의 간격 중앙값은 **0.18초**, 59%가 0.5초 미만이다.
조사관 4명이 병렬이라 `researcher.read`가 한꺼번에 쏟아진다. 반면 4.2절 연출은
`say` 3초, 책 꺼내는 모션까지 붙는다. **연출이 이벤트보다 10배 이상 느리다.**
`ts`를 그대로 재생하면 애니메이션이 끝없이 밀린다. 12편의 길이도 15~37초로 2.5배 차이 난다.

그래서 **`ts`는 순서 판정에만 쓰고, 재생 시간은 연출이 정한다.**

#### 큐를 에이전트별로 나눈다

병렬성은 이 작품의 주제다. 전역 큐 하나로 직렬화하면 팬아웃이 화면에서 사라진다.

```
r0 큐: [read, read, read, done]      ─┐
r1 큐: [read, read, done]             ├─ 서로 독립적으로, 동시에 흐른다
r2 큐: [read, read, read, fix, done]  │
r3 큐: [read, done]                  ─┘

전역 큐: [run.start, plan.done, dispatch] ─ 배리어 ─ [review] ─ 배리어 ─ [synthesize, evaluate, run.end]
```

- 각 에이전트는 **자기 큐를 순차 소비**한다. 앞 연출이 끝나야 다음이 시작된다.
- 에이전트끼리는 **동시에 흐른다**. 4명이 각자 다른 속도로 읽는 모습이 그대로 보인다.
- 전역 이벤트는 **배리어**다. 모든 에이전트 큐가 빌 때까지 기다린 뒤 실행한다.
  `dispatch`는 배리어를 풀고 4명을 동시에 출발시키는 신호다 — 팬아웃이 보이는 순간.
- 재위임(`바퀴:2`)은 `dispatch`가 다시 오면서 **지정된 인덱스만** 큐를 다시 받는다.

#### 연출 지속시간

| 이벤트 | 지속 | 비고 |
|---|---|---|
| `run.start` | 1.0초 | 질문 띄우기 |
| `plan.done` | 3.0초 | 카드 4장을 0.4초 간격으로 부착 |
| `dispatch` | 이동 시간 | 거리 ÷ 속도. 배리어 해제 |
| `researcher.read` | 2.5초 | 이동 0.8 + 책 1.0 + 메모 축소 0.7 |
| `researcher.read(관련:false)` | 1.5초 | 책 덮고 빈손 |
| `researcher.fix` | 2.0초 | 빨간 딱지 → 고쳐 쓰기 |
| `researcher.done` | 2.0초 | 책상 복귀 + 원고 올리기 |
| `review` | 2.0초 | 빈칸 점검 |
| `synthesize` | 2.0초 | 조판대 |
| `evaluate` | 3.0초 | 계기판 8개가 차례로 |
| `run.end` | — | 정지 |

**추정 재생 길이 25~30초.** 원본 15~37초의 편차가 흡수된다.
배속은 `1× / 2× / 즉시`. 즉시는 연출을 건너뛰고 최종 상태만 그린다.

> **왜 실시간 재생을 버렸나** — 실시간은 "실제로 얼마나 걸렸나"를 보여 준다.
> 그러나 이 화면이 보여 줄 것은 시간이 아니라 **구조**다(팬아웃·격리·재위임).
> 0.18초 간격으로 지나가는 것은 아무것도 설명하지 못한다. 대신 실제 소요 시간은
> 계기판에 숫자로 적는다.

---

### 5.6 좌표계 — 실측으로 확정

**에셋 실측** (2026-09-22)

```
library 88파일 · floor 28파일   전부 캔버스 256×512 · 4방향 _N/_E/_S/_W
floor_N 불투명 영역 (0,383)-(256,512)  →  폭 256 · 높이 129   ← 정확히 2:1 다이아몬드
block_N 불투명 높이 286                →  바닥 128 + 입면 158
bookcaseBooks_N 불투명 (5,288)-(173,482) → 폭 168 · 높이 194
```

바닥면이 **캔버스 하단에 정렬**돼 있다. 앵커를 좌하단으로 잡으면 모든 오브젝트가
같은 식으로 놓인다.

#### 투영식

타일 단위 `W=256`, `H=128` (스케일 `s`는 화면 폭에 맞춰 0.5 기본).

```js
// 타일 (col,row) → 다이아몬드 바닥 중심의 화면 좌표
screenX = originX + (col - row) * (W / 2)      // ±128
screenY = originY + (col + row) * (H / 2)      //  +64

// 256×512 스프라이트를 그리는 좌상단 위치
drawX = screenX - W / 2                        // -128
drawY = screenY + H / 2 - 512                  // 바닥 꼭짓점을 맞춘다
```

**z-정렬**: `col + row` 오름차순. 같으면 `층`(바닥 0 → 가구 1 → 캐릭터 2) 순.
캐릭터는 타일 중심에 발을 두고 `drawY = screenY - 캐릭터높이`.

#### 배치표 (격자 10×8)

5.1절 레이아웃을 좌표로 옮긴 것이다. 서고 사이를 두 칸씩 띄워 이동이 눈에 보이게 한다.

| 오브젝트 | (col,row) | 스프라이트 |
|---|---|---|
| 화이트보드 (목차) | (1,0) | `displayCase_S` |
| 소장 책상 | (1,2) | `longTableChairs_S` |
| 검토 테이블 | (1,4) | `longTableDecoratedChairsBooks_S` |
| 조판대 | (1,6) | `bookStand_S` |
| 계기판 | (4,7) | `displayCaseBooks_S` |
| 서고 `유형론` | (5,0) | `bookcase*_S` |
| 서고 `성격심리` | (8,0) | `bookcase*_S` |
| 서고 `측정` | (5,2) | `bookcase*_S` |
| 서고 `편향` | (8,2) | `bookcase*_S` |
| 서고 `유사사례` | (5,4) | `bookcase*_S` |
| 서고 `관계` | (8,4) | `bookcase*_S` |
| 조사관 책상 r1~r4 | (6,7)(7,7)(8,7)(9,7) | `libraryChair_S` |

서고 이름 6개는 **이벤트의 `서고` 값과 그대로 일치한다**(12편 실측 확인).
매핑 테이블이 따로 필요 없다.

배치표의 스프라이트 이름은 **전부 실제 파일 존재를 확인했다**(2026-09-22). 처음 적었던 `deskCorner`·`tableLong`은 없는 이름이라 `longTableChairs`·`longTableDecoratedChairsBooks`로 바꿨다.

서고 스프라이트는 5.4절대로 `bookcaseEmpty_S`(아무도 안 읽음) ↔ `bookcaseHalfBooks_S`
↔ `bookcaseBooks_S`(여러 번 읽힘)를 읽은 횟수에 따라 바꿔 끼운다.
`관계` 서고는 실측상 10회 등장하므로 비어 있지 않다 — 14.3 참조.

#### 벽 — 늘 화면 뒤쪽 두 변에

벽은 **논리 좌표에 고정하지 않는다.** 고정하면 90° 돌렸을 때 벽이 앞으로 나와 방 안을 가린다.
회전된 격자의 `rc=0`·`rr=0` 두 변에 매번 다시 세우고, 스프라이트도 회전을 따라가지 않는다
(`_E`는 화면 왼쪽 위 변, `_S`는 오른쪽 위 변 — 실측).

| 자리 | 조각 |
|---|---|
| 모서리 `(0,0)` | `wallCorner_S` — **한 장이 두 변을 덮는다** |
| 두 변 | `wall_E` · `wall_S` |
| 각 변 4번째 칸 | `doorway_E` · `doorway_S` |
| 각 변 7번째 칸 | `window_E` · `window_S` |

> **모서리를 맞추는 데 두 번 실패했다.** Library 팩의 `wallBooks`로 두 벽을 세우면 모서리에서
> 책장이 서로를 뚫고 나온다. 비우면 한 칸이 뚫리고, 한쪽만 이으면 삼각형 틈이 남는다.
> 그리는 순서를 바꿔 겹침을 가려 봐도 여전히 어긋났다. **원인은 배치가 아니라 조각이 없는 것**
> 이었다 — Library 팩에는 코너 조각이 아예 없다. Prototype 팩의 `wallCorner`로 바꾸고서야 맞았다.
> 그 팩은 전부 프로토타입 주황이라 색만 바꿔 쓴다(`tools/리컬러.py`, CC0).

> **검증 방법** — 이 좌표는 계산으로 얻은 것이고 아직 눈으로 본 적이 없다.
> 구현 첫 걸음은 **바닥 + 서고 6개만 그린 스크린샷**이다. 겹침·여백을 거기서 보정한다.
> 캐릭터·애니메이션은 그다음이다.

---

### 5.7 에이전트 식별 — 인덱스가 ID다

**이벤트에는 `r1`~`r4`가 없다.** 조사관을 가리키는 것은 `절` 이름뿐이다.

```jsonc
{ "type":"dispatch", "바퀴":1, "인원":4, "배치":[0,1,2,3] }   // 목차 인덱스
{ "type":"dispatch", "바퀴":2, "인원":1, "배치":[3] }          // 재위임 — 4번만 다시
{ "type":"researcher.start", "절":"융의 유형론", … }            // 이름으로만 온다
```

규칙:

1. **`agentId` = `plan.done.목차`의 인덱스** (0~3). 화면 표시명은 `r1`~`r4`.
2. `plan.done`을 받을 때 `절 제목 → 인덱스` 테이블을 만든다. 이후 모든
   `researcher.*` 이벤트는 이 테이블로 에이전트를 찾는다.
3. `dispatch.배치`는 이미 인덱스 배열이므로 그대로 쓴다.
4. 소장은 `coord`. 인덱스 체계 밖이다.

> **절 제목을 키로 쓰면 안 되는 이유** — 12편에서는 모든 절 제목이 고유했다(실측).
> 그러나 13.5에 기획 노드가 같은 제목을 두 번 낸 사례가 기록돼 있다.
> 제목이 겹치면 두 조사관이 한 캐릭터로 합쳐져 화면이 조용히 틀린다.
> **겹치면 먼저 나온 순서로 배정하고 콘솔에 경고를 남긴다.**

---

### 5.8 캐릭터 조립 — 파일명은 대응표와 다르다

5.4절의 대응표는 의미 이름(`happy`·`eye_half`)으로 적혀 있으나, **실제 파일명은 다르다.**

```
몸통  {color}_body_{shape}.png        80×80    색 6 × 몸통 4
손    {color}_hand_{pose}.png         ~34×38   색 6 × 포즈 6
얼굴  face_a.png … face_l.png         ~50×29   완성 얼굴 12종
부품  facial_part_mouth_happy.png     20×7     입 4종
      facial_part_eye_half_top.png    20×10    눈 8종 · 눈썹 4종
```

**부품 조립을 쓴다.** 완성 얼굴 `face_a~l`은 어떤 표정인지 이름으로 알 수 없어
대응표를 만들 수 없다. 부품은 이름이 곧 의미다.

레이어 순서와 기준 오프셋 (몸통 80×80의 좌상단 기준):

| 레이어 | 파일 | 오프셋 |
|---|---|---|
| 1 몸통 | `{color}_body_{shape}` | (0,0) |
| 2 눈 왼 | `facial_part_eye_*` | (18,26) |
| 2 눈 오 | `facial_part_eye_*` | (42,26) |
| 3 입 | `facial_part_mouth_*` | (30,50) |
| 4 손 왼 | `{color}_hand_{pose}` | (-22,38) 좌우 반전 |
| 4 손 오 | `{color}_hand_{pose}` | (68,38) |

오프셋은 **기준값이다.** 5.6절과 마찬가지로 캐릭터 한 명을 그려 본 스크린샷에서 보정한다.

배역:

| 배역 | 색 · 몸통 |
|---|---|
| 소장 `coord` | `purple` · `squircle` |
| 조사관 `r1`~`r4` | `blue` / `green` / `pink` / `red` · `circle` |

상태 → 부품 (5.4절 대응표를 실제 파일명으로 옮긴 것):

| 상태 | 눈 | 입 | 손 |
|---|---|---|---|
| 읽는 중 | `eye_half_top` | — | `closed` (메모 소지) |
| 관련 없음 | `eye_closed_down` | — | `open` (빈손) |
| 충분 신고 | `eye_open` | `mouth_happy` | `thumb` |
| 부족 신고 | `eye_open` | `mouth_sad` | `open` |
| **인용 0곳 — 경보** | `eye_open` | `mouth_angry` | `open` + 빨간 테두리 |
| 재위임 복귀 | `eye_open` | `mouth_sad` | `peace` |
| 절 완성 | `eye_half_top` | `mouth_smirk` | `thumb` |

---

## 6. 평가 지표 — 계기판

정답표도 판정 모델도 쓰지 않는다. **만들어진 글과 실제로 읽은 문서만** 본다.

| 지표 | 계산 | 어느 장치의 계기 | 종류 |
|---|---|---|---|
| 근거율 | 문장 중 «문서명» 붙은 비율 | 깊이 · 재위임 | 잣대 |
| **인용 0곳 절 수** | `sum(1 for x in 절 if not x["인용"])` | **절 단위 근거 구멍** | **그물** |
| 허위 인용 | 읽지 않은 문서를 인용했나 | 경보 — 0이 아니면 탈락 | 그물 |
| **출처 불일치** | 인용 문서 원문에 그 문장의 고유명사·연도가 실제로 있나 | **인용의 타당성** | 잣대 (관찰 중) |
| 읽고 안 쓴 문서 | 읽었으나 보고서에 미등장 | 배정 품질 | 잣대 |
| 최다 문서 편중 | 인용이 한 문서에 몰린 비율 | 배정 · 구역 | 잣대 |
| 중복률 | 같은 문서를 두 절 이상이 읽음 | 구역 | 잣대 |
| 격리율 | 코디네이터가 본 글자 ÷ 팀 전체 | 격리 — 한 자릿수여야 정상 | 잣대 |

**「인용 0곳 절 수」를 추가한 이유** — 보고서 전체 근거율이 60%여도, 네 절 중 한 절이 0곳이면 그 절은 통째로 근거 없는 글이다. 전체 평균에 묻혀 안 보인다. 우리 주제가 "근거 없는 주장을 검증하는 것"이므로, **절 단위로 구멍을 잡는 그물**이 반드시 필요하다. (14강 더해보기 2번)

**「출처 불일치」를 추가한 이유** — 근거율은 «»가 **붙어 있는지**만 잰다. 붙은 근거가 **맞는지**는 재지 못한다. 실제로 «16PF Questionnaire» 원문에 한 번도 나오지 않는 아담 그랜트 발언에 그 문서가 인용된 사례를 잡았다(2026-09-22 실측). 판정 모델은 쓰지 않는다. 문장 속 영문 고유명사·4자리 연도가 인용된 문서 원문에 글자 그대로 있는지 대조할 뿐이다.

> **한계** — 한국어 표기만 있고 영문 병기가 없는 이름은 놓친다. 그래서 그물이 아니라 잣대로 둔다.

### 6.1 그물과 잣대 분리

- **그물(자동 탈락 기준)**: 허위 인용 > 0 · 인용 0곳 절 > 0 · 편중 > 50%
- **잣대(순위 비교)**: 근거율 · 출처 불일치 · 읽고 안 쓴 문서 · 중복률 · 격리율
- 그물을 통과한 것만 사람이 읽고 고른다 (16강)

---

## 7. 절제 실험 계획

배포 콘텐츠가 곧 절제 실험 결과다.

### 7.1 질문 3개 (성격이 다른 것으로)

| 유형 | 질문 | 기대 |
|---|---|---|
| **단일** | "바넘 효과란 무엇인가?" | 답이 한 문서에 몰려 있음 → **나누면 손해나는 사례** |
| **다갈래** | "MBTI 궁합론은 어떤 근거로 제시되며, 심리학계는 이를 어떻게 평가하는가?" | 여러 서고에 흩어짐 → **이 구조가 가장 잘 맞는 유형** |
| **관계** | "성격이 비슷한 사람끼리 실제로 더 잘 맞는가?" | `Assortative mating`(고립 문서)에 닿아야 함 → **배정 품질 시험** |

### 7.2 설정 4개

| 이름 | 끄는 것 | 무엇을 보려는가 |
|---|---|---|
| 기본 | — | 기준선 |
| 배정·구역 끔 | `배정`, `구역` | 중복률·편중이 뛰는가 |
| 역할 끔 | `역할` | **정말 차이가 없는가** (17강 결론 재현) |
| 재위임 끔 | `재위임` | 근거율이 떨어지는가 |

**3 × 4 = 녹화 12편.** 방문자가 스위치를 토글하면 움직임 자체가 달라진다.

---

## 8. 배포

### 8.1 방식

| 항목 | 선택 |
|---|---|
| 호스팅 | Vercel |
| 기본 경험 | **정적 재생** — 빌드 없음, 백엔드 없음, 비용 0 |
| 라이브 | Vercel Python Function + SSE (방문자 본인 키) |

### 8.2 API 키 취급 규칙

방문자가 **설정 화면에서 자기 키를 입력**한다.

- 키는 **브라우저 `localStorage`에만** 저장한다
- 라이브 실행 시 **요청 헤더로만** 전달한다
- 서버는 **메모리에서만 사용**하고 로그·DB·응답 어디에도 남기지 않는다
- 저장소에는 어떤 키도 커밋하지 않는다 (`.gitignore` 설정 완료)

---

## 9. 디렉터리 구조

```
Deep-research-agent/
├─ data/
│  └─ corpus.json              # 문서 52건 + 링크 (생성 완료)
├─ pipeline/                   # Python · LangGraph
│  ├─ graph.py                 # 6노드 + 팬아웃 + 루프
│  ├─ nodes.py                 # plan/dispatch/researcher/review/synthesize/evaluate
│  ├─ metrics.py               # 계기판 7개
│  ├─ events.py                # emit() — queue 기반 (병렬 안전)
│  └─ record.py                # 녹화 생성기 (질문3 × 설정4)
├─ runs/                       # 녹화된 이벤트 스트림 12편
├─ tools/
│  ├─ render_iso.py            # Furniture Kit 3D → Miniature 규격 렌더 (Blender)
│  └─ 보정.py                  # 렌더 결과 색 보정
├─ web/                        # 의존성 0 · Canvas
│  ├─ index.html               # 재생 UI
│  ├─ runs/                    # 녹화 12편 (fetch 용 사본)
│  ├─ assets/
│  │  ├─ characters/           #   Shape Characters 104
│  │  ├─ furniture/            #   직접 렌더한 소품 26종×4방향
│  │  └─ CREDITS.md
│  └─ src/
│     ├─ config.js             # 좌표계·배치표·서고
│     ├─ iso.js                # 투영식·깊이
│     ├─ actors.js             # 캐릭터 조립 (몸통+얼굴+손)
│     ├─ renderer.js           # 그리기·게이지·말풍선
│     ├─ player.js             # 재생 엔진 (큐·배리어·보간)
│     └─ view.js               # 카메라 (이동·확대)
├─ api/                        # Vercel Python Function (라이브 모드)
├─ docs/
│  └─ 프로젝트 계획서.md
└─ .gitignore
```

---

## 10. 로드맵

| 단계 | 할 일 | 완료 기준 |
|---|---|---|
| **0** | ~~주제·코퍼스 확정~~ | ✅ 52건 · 170만 자 · 링크 485 |
| **0b** | ~~에셋 확보~~ | ✅ Kenney CC0 220파일 · `web/assets/` |
| **1** | ~~걸어 다니는 뼈대~~ | ✅ 빈 노드 6개로 한 바퀴 · 전부 0 · 그물이 뼈대를 탈락시킴 |
| **2** | ~~노드 6개 채우기~~ | ✅ 한국어 보고서 2,520자 · 그물 통과 |
| **3** | ~~`emit()` 삽입~~ | ✅ 이벤트 34~44개 · 계획서 4.1 스펙대로 |
| **4** | ~~녹화 12편 생성~~ | ✅ `runs/{질문}-{설정}.json` 12편 · 그물 통과 10 / 탈락 2 · ₩356 |
| **5** | ~~시각화 웹앱~~ | ✅ 재생 모드로 캐릭터가 움직인다 · 12편 12~28초 (15장) |
| **6** | 라이브 모드 | 본인 키로 실행된다 |
| **7** | Vercel 배포 | 공개 URL |

**순서가 중요하다.** 시각화부터 만들면 이벤트 스펙이 계속 바뀐다. 파이프라인 → 녹화 → 시각화 순으로 간다.

---

## 11. 리스크와 대응

| 리스크 | 징후 | 대응 |
|---|---|---|
| 영문 원문 → 한국어 보고서 품질 저하 | 번역투, 용어 불일치 | `read_one()` 프롬프트에 "한국어로 간추려라" 명시. 용어 대조표 프롬프트에 포함 |
| 문서가 너무 길어 요약 비용 증가 | 평균 34K자 (원본의 2.6배) | `cap=60000` 유지. **실측: `Carl Jung`(93K)·`Big Five`(91K)는 뒷부분이 잘리고, 두 문서 모두 "관련 없음" 판정을 자주 받는다.** 잘림이 원인인지는 미확인 |
| 고립 문서에 아무도 안 닿음 | `Assortative mating` 미등장 | **이건 실패가 아니라 관찰 대상.** 카드 품질 지표로 보고. **실측: 6회 실행 모두 「관계」 서고 0건 — 예측대로 아무도 닿지 못했다** |
| 절 사이 중복·모순 | 같은 사실이 두 절에 | 고치지 않는다 (12강 결정). 한계로 명시 |
| 라이브 모드 비용 폭주 | 방문자가 반복 실행 | 방문자 본인 키 사용 + 프런트에서 동시 실행 1회 제한 |
| 배포 후 녹화가 낡음 | 코퍼스 갱신 시 | 녹화에 `built` 날짜 표기. 재생성 스크립트 제공 |

---

## 12. 이 프로젝트가 강의에서 가져온 여섯 결정

| 노드 | 결정 | 근거 |
|---|---|---|
| ① 기획 | 제목만이 아니라 **문서 카드**(제목+앞 250자)를 보여준다 | 코퍼스의 소량으로 배정 품질이 달라진다 |
| ② 배치 | **4절 × 3건** | 장문 보고서가 목표. 짧은 답이면 4절×2건이 맞다 |
| ③ 조사 | 요약이 아니라 **절 원고**를 올려받는다 | 원문을 읽은 사람이 써야 인용이 정확하다 |
| ④ 점검 | 조사관의 **자기신고**로 루프를 연다 | 무엇이 비었는지를 문장으로 얻는 유일한 공짜 방법 |
| ⑤ 종합 | 절을 **다시 쓰지 않는다** | 문체를 포기하고 인용을 지킨다 |
| ⑥ 평가 | 정답표도 판정 모델도 **쓰지 않는다** | 코퍼스를 갈아 끼워도 그대로 돈다 |

---

## 13. 구현 실측 기록 (2026-09-22)

계획서가 예측한 것과 **실제로 돌려 보니 달랐던 것**을 남긴다. 추측으로 고치지 않기 위해서다.

### 13.1 원고 프롬프트 A/B — 규칙보다 본보기

같은 메모 4세트로 원고 작성 프롬프트만 바꿔 2회씩 측정했다.

| 변형 | 근거율 (1회) | (2회) | 요지 |
|---|---|---|---|
| V0 현행 | 27.0% | 29.4% | "문장마다 «문서명»을 붙인다" 한 줄 |
| V1 인용 강제 | 17.9% | 22.2% | ★로 강조 + 자기 점검 요구 |
| **V2 한 문장 한 사실** | **96.7%** | **100.0%** | **잘된 문장 2개 · 잘못된 문장 2개를 본보기로 제시** |

**규칙을 강하게 쓴 V1이 오히려 나빴다.** 모델은 지시의 강도가 아니라 **본보기**에 반응했다. V2 채택.

부작용: 인용을 강제하니 모델이 자기 추론 문장에도 «»를 갖다 붙였다. 금지 예시를 프롬프트에 그대로 넣어도 재발했다. 프롬프트로는 완전히 못 막는다 → **지표(출처 불일치)로 잡는다**.

### 13.2 잡은 버그 2개 — 둘 다 구현 중 만든 것

| 버그 | 증상 | 원인 | 수정 |
|---|---|---|---|
| 메모 유실 | 재위임을 거친 절만 근거율 25% | 2바퀴 조사관이 1바퀴 메모를 못 물려받아, 1바퀴에 읽은 문서를 인용할 자격을 잃었다 | 메모 본문을 상태에 보존해 다음 바퀴로 넘긴다 |
| **오귀속 유발** | 아담 그랜트 발언에 «16PF Questionnaire» 인용 | 허위 인용 교정 프롬프트가 "허용 목록 문서로 **근거를 바꿔 달라**"고 지시했다 | "통째로 **지워라**. 다른 문서 이름으로 바꿔 달지 마라" |

두 번째가 더 나쁘다. **허위 인용 0이 지표를 속여 만든 값이었다.** 원문 대조로 확인했다 — «16PF Questionnaire»에 "Grant" 0회, «Myers–Briggs Type Indicator»에 3회.

교훈: **지표를 만족시키라고 시키면 모델은 지표를 만족시킨다.** 지표가 재지 못하는 쪽으로.

### 13.3 재위임은 범인이 아니었다

"재위임 절의 근거율이 낮다"는 관찰에서 "재위임이 인용 규칙을 희석한다"고 의심했으나, 같은 직전 원고를 주고 A/B 한 결과 **현행·개선 모두 100%** 였다. 재위임 절이 나빠 보인 것은 **선택 편향**이다 — 점검이 원래 부실한 절만 되돌려 보내기 때문이다. 진짜 원인은 13.2의 메모 유실이었다.

### 13.4 기준선 (기본 설정 · 재현 3회)

```
근거율 73.3 ~ 96.4% (평균 82.7%)   인용0곳절 0   허위인용 0   출처불일치 0
격리율 1.9%   중복률 15~31%   보고서 2,520 ~ 2,770자
21~26 호출 · 입력 약 16만 토큰 · 편당 ₩34~46 · 30~40초
```

격리율 1.9%는 계획서가 요구한 "한 자릿수"를 만족한다. 컨텍스트 격리는 단일 조사관 기준 **원문 94,675자 → 메모 631자 (0.7%)**.

### 13.5 아직 안 고친 것 (절제 실험이 측정할 대상)

- **「관계」 서고 0건** — `Assortative mating`에 6회 모두 아무도 닿지 못했다. 계획서 2.5절 예측대로.
  → **14.3에서 뒤집혔다.** 질문을 「관계」형으로 바꾸자 네 설정 모두 닿았다. 배정의 실패가 아니라 **질문 어휘의 문제**였다.
- **`cap=60000` 잘림** — `Carl Jung`(93K)·`Big Five`(91K)가 자주 "관련 없음" 판정을 받는다. 잘림이 원인인지 미확인.
- **절 제목 중복** — 기획 노드가 같은 제목을 두 번 낸 사례 1건.
- **구역 분할의 대가** — 시작 문서만 나누고 프런티어는 안 나눈다. 두 절이 같은 문서를 각각 읽고 둘 다 "관련 없음" 판정한 사례가 있다.

---

## 14. 절제 실험 실측 (2026-09-22)

7장의 계획을 실제로 돌린 결과다. 생성기는 `pipeline/record.py`, 산물은 `runs/{질문}-{설정}.json` 12편.
**총 ₩356 · 4분 15초 · 그물 통과 10 / 탈락 2.**

### 14.1 근거율 12칸

| 질문 | 기본 | 배정·구역끔 | 역할끔 | 재위임끔 |
|---|---|---|---|---|
| **단일** (바넘 효과) | 0.57 | 0.78 ❌허위인용 | 0.55 | 0.59 |
| **다갈래** (MBTI 궁합) | 0.93 | 0.75 | **1.00** | 0.82 ❌인용0곳절 |
| **관계** (유유상종) | 0.85 | 0.79 | 0.90 | 0.91 |

❌는 그물 탈락. 탈락 2편은 사람이 읽을 후보에서 자동으로 빠진다(6.1절).

### 14.2 기대대로였던 것 — 「단일 질문은 나누면 손해」

단일 질문은 근거율 0.55~0.59로 세 질문 중 최저이고, 중복률 0.38로 최고다.
답이 한 문서에 몰려 있는데 4절로 쪼개니 **같은 문서를 네 번 읽는다**.
배정·구역을 끄면 중복률이 0.38 → 0.50, 편중이 0.29 → 0.43으로 뛴다 —
7.2절이 예측한 그대로다. 다만 **이 상승은 단일 질문에서만 나타났고**,
다갈래·관계에서는 오히려 내려갔다.

### 14.3 어긋난 것 ① — 고립 문서 도달에 변별력이 없었다

7.1절은 「관계」 질문을 **배정 품질 시험**으로 설계했다. 고립 문서 `Assortative mating`은
링크로 닿을 수 없으니, 배정이 제대로 일해야만 도달한다는 가정이었다.

실측은 **네 설정 모두 도달**이었다. 배정·구역을 끈 편까지 닿았고, 넷 다 보고서에 인용했다.
배정을 끄고도 닿는다면 그 도달은 배정이 한 일이 아니다.
질문 자체의 어휘가 문서 제목과 가까워서 검색이 곧장 집어 온 것으로 보인다.

→ **이 질문은 배정 품질을 재지 못한다.** 13.5의 「관계 서고 0건」이 배정의 실패로 보였던 것도
같은 이유의 뒷면이었다: 문제는 배정이 아니라 **질문이 무엇을 부르느냐**였다.
배정 품질을 재려면 제목과 어휘가 겹치지 않는 고립 문서를 따로 골라야 한다.

### 14.4 어긋난 것 ② — 역할을 끈 쪽이 더 나은 경우가 2/3

7.2절은 역할 끔에 대해 "**정말 차이가 없는가**(17강 결론 재현)"를 물었다.
실측은 차이 없음을 넘어 **끈 쪽이 나은 방향**으로 나왔다 — 다갈래 1.00 vs 0.93, 관계 0.90 vs 0.85.
기본이 이긴 것은 단일(0.57 vs 0.55)뿐이고 그 차이는 0.02다.

**단, 이것은 각 칸이 1회 실행인 n=3 관찰이다.** 13.4의 기준선이 같은 설정에서
73.3~96.4%로 흔들렸음을 기억해야 한다. 그 폭이면 0.93과 1.00은 구별되지 않는다.
**"역할을 끄는 게 낫다"고 쓰면 안 된다.** 지금 말할 수 있는 것은
"역할을 켜서 좋아졌다는 증거가 이 12편에 없다"까지다.

→ 5장 시각화와 배포 콘텐츠의 문구는 여기에 맞춘다. 단정하려면 칸마다 3회씩 재야 한다(₩1,100 · 15분).

### 14.5 남은 숙제

- **역할 축 반복 측정** — 위 이유로 n=1은 결론을 못 낸다.
- **배정 품질을 잴 질문 다시 설계** — 제목 어휘가 겹치지 않는 고립 문서 기준으로.
- **탈락 2편의 원인 확인** — 단일·배정구역끔의 허위 인용 1건, 다갈래·재위임끔의 인용 0곳 절 1건.
  재위임을 끄면 근거가 빈 절이 생긴다는 7.2절 가설과 방향이 맞지만, 역시 1건이다.

---

## 15. 시각화 구현 실측 (2026-09-22)

5단계를 만들며 겪은 것. 결론부터: **소품은 기성 스프라이트를 쓰지 못하고 3D 원본을 다시 렌더했다.**

### 15.1 왜 Blender 로 다시 렌더했나

처음에는 Isometric Miniature Library 를 썼다. 정렬은 맞았지만 어두운 사실적 나무라
플랫한 도형 캐릭터와 섞이지 않았다. 팩을 갈아 봐도 해결되지 않았다 —
2D 아이소메트릭에 사무실 가구가 있는 팩은 그것뿐이고, 나머지는 픽셀아트(ThirdAngle)
아니면 라이선스 미표기(AI 생성)였다.

**Furniture Kit** 은 스타일이 맞았다(밝은 파스텔, 플랫 셰이딩). 그런데 기성 Isometric PNG 로
방을 지으려 하자 벽과 가구가 계속 어긋났다. 원인은 문서에 있었다:

```xml
<!-- Isometric Miniature 시리즈의 Tiled 샘플 -->
<map orientation="isometric" tilewidth="256" tileheight="128">
<tileset tilewidth="256" tileheight="512">
```

Miniature 시리즈는 **256×512 균일 캔버스**라 셀에 얹기만 하면 맞는다.
Furniture Kit 은 오브젝트마다 다르게 크롭돼 있어 **격자에 맞출 기준점이 없다**
(투영비도 2:1 이 아니라 2.81:1). 3D 모델 원점으로 역산까지 해 봤지만
`wall`·`bookcase` 는 맞고 `desk`·`chair`·`floorFull` 은 틀렸다. 복원 불가능하다.

그래서 **3D 원본을 Miniature 규격으로 다시 렌더했다**(`tools/render_iso.py`).
파라미터는 추측이 아니라 기존 Miniature 타일과 대조해 역산했다:

```
2:1 정투영 · 카메라 X 60° Z 45°/135°/225°/315°
ortho_scale = 2√2 × (H/512)      ← 타일 폭이 256px 이 되도록
shift_y     = 0.4267             ← 다이아몬드 허리를 y=704 에 맞추도록
캔버스 384×768                    ← 256 으로 하면 책장이 프레임 밖으로 나가 잘린다

검증: floorFull 불투명 (0,382,256,512) · 허리 y=704 · 폭 256  ↔  기준과 일치
```

Workbench STUDIO 는 면별 음영을 남기지만 전체가 어두워진다(바닥 `#99816c`, 원본 `#ffcb81`).
밝기·채도를 원본에 맞춰 보정하고, 바닥만 채도를 더 낮춰 가구가 묻히지 않게 했다(`tools/보정.py`).

> **계획서 0b 의 판단을 뒤집은 셈이다.** 그때 3D 팩을 "렌더링 공정 필요"로 탈락시켰는데,
> 실제로 해 보니 스크립트 한 번이면 되고 규격을 우리가 정할 수 있다는 것이 오히려 이점이었다.

### 15.2 세 번 헤맨 것 — 그리기 규칙 셋

같은 증상을 엉뚱한 곳에서 찾은 기록이다. 코드에 주석으로 남겨 뒀다.

| 증상 | 처음 의심한 것 | 실제 원인 |
|---|---|---|
| 책장 다리가 잘린다 | 스프라이트가 잘렸나 → 비율인가 → 방향인가 | **그리는 순서.** 앞 타일 바닥이 뒤 오브젝트의 다리를 덮었다 |
| 탁자가 바닥에서 뜬다 | 앵커 계산 | **비율.** 세로만 누르면 아이소메트릭이 깨진다 |
| 책이 선반에서 밀린다 | 배율을 따로 걸어서 | **오프셋 값이 눈대중이었다.** 3D 모델에서 계산하니 맞았다 |

세 줄로 줄이면:

1. **한 축만 늘리지 않는다.** 비율을 깨면 뜨거나 겹친다
2. **바닥은 깊이순에 섞지 않는다.** 평면이므로 먼저 전부 깔고 나머지를 올린다
3. **두 요소에 배율을 따로 걸지 않는다.** 합성해 한 장으로 만든다

### 15.3 서고를 계기 기계로 바꾼 이유

계획서 5.4 는 서가를 `bookcaseBooks ↔ bookcaseEmpty` 로 바꿔 채움을 보이려 했다.
실제로 만들어 보니 **변화가 작고 몇 권까지밖에 못 센다.** 가구 모양을 바꾸면
'채움'이 아니라 다른 가구처럼 보이고, 크기가 다른 가구를 섞으면 벽면이 어수선해진다.

그래서 서고를 **계기가 달린 기계**(`washerDryerStacked`)로 바꾸고,
읽은 횟수를 **게이지 막대로 그린다**(Canvas). 몇 번을 읽든 정확히 보이고 서고별 색으로 구분된다.
실측상 서고별 편차가 크므로(편향 38회 vs 측정 9회) 막대 길이 차이가 그대로 드러난다.

### 15.4 재생 — 설계대로 동작한다

5.5 절 설계(에이전트별 큐 + 전역 배리어)를 그대로 구현했다. 12편 전부 끝까지 재생되고
계기판이 채워진다. **재생 길이 12~28초** — 원본 15~37초의 편차가 흡수됐다.

```
다갈래·기본  28.3초 · 편향 7 · 유사사례 3 · 유형론 1 · 관계 1
단일·기본    12.4초 · 편향 4 · 유사사례 1 · 유형론 1
```

편중이 게이지 길이로 바로 보인다. 다갈래·기본에서 편향 기계만 7칸까지 차오르는 동안
측정·성격심리는 0 이다.

### 15.5 아직 안 한 것

- **4.2 절 연출 일부** — "두꺼운 책 → 얇은 메모"(컨텍스트 격리), 재위임 빨간 딱지,
  소장의 화이트보드·조판대 이동. 지금은 표정과 말풍선으로만 표현한다
- **회전** — 만들었다가 뺐다. 한 방향 고정이 읽기 쉽고 스프라이트도 1/4 로 준다
- **계기판 위치** — 가구가 아니라 화면 패널로 띄웠다. 아이소메트릭 모니터 안에 숫자를 그리면 안 보인다
