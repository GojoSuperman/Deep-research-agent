# 딥리서치 에이전트 (MBTI 검증) — 프로젝트 컨텍스트

모두의연구소 8강 「딥리서치 에이전트 만들기 — 오케스트레이션」 수업 과제.
**LangGraph 6노드 딥리서처 + 아이소메트릭 시각화 웹앱**을 만들어 Vercel에 배포한다.

> 📄 **전체 설계는 `docs/프로젝트 계획서.md` (416줄, 12장)에 있다. 작업 전에 먼저 읽을 것.**

---

## 확정된 것 (재논의 불필요)

| 항목 | 결정 | 근거 |
|---|---|---|
| **주제** | "MBTI 궁합론은 어떤 근거로 제시되며, 심리학계는 이를 어떻게 평가하는가?" | 유형론 비판 = 근거 인용 장치의 의미를 증폭 |
| **코퍼스** | 영문 위키백과 52건 (`data/corpus.json`) | 한국어판은 16유형이 전부 리다이렉트라 탈락 |
| **보고서 언어** | 한국어 (원문은 영문) | — |
| **설정** | 4절 × 3건, 최대 2바퀴 | 장문 보고서가 목표 |
| **에셋** | Kenney CC0 (`web/assets/`) | 도형 캐릭터 — 사람 쓰면 "무슨 유형처럼 생겼나" 해석이 붙음 |
| **배포** | Vercel · 재생 모드 기본 + 방문자 본인 키 라이브 | 공개 배포 시 키·비용 리스크 해결 |
| **평가 지표** | 강의 6개 + **「인용 0곳 절 수」** 추가 | 절 단위 근거 구멍을 잡는 그물 |

## 코퍼스 실측값

```
52건 · 1,705,434자 · 링크 485개 (문서당 9.3)
추정 852,700 토큰 = gpt-4o-mini 창(128,000)의 6.7배
고립 문서 1건: Assortative mating  ← 버그 아님. 링크로 못 닿는데 질문엔 꼭 필요한 문서
서고 6분류: 측정 11 · 유형론 11 · 성격심리 11 · 유사사례 7 · 편향 6 · 관계 6
```

## 현재 상태 (2026-09-22 갱신)

- ✅ 0 주제·코퍼스 확정 · 0b 에셋 · 계획서
- ✅ **1~3 파이프라인 완성** — 6노드가 한국어 보고서를 만든다 (커밋 `277aeea`)
  - 기준선: 근거율 73~96% · 허위인용 0 · 출처불일치 0 · 격리율 1.9% · 편당 ₩40
  - 실측 기록은 **계획서 13장**에 (프롬프트 A/B, 잡은 버그 2개, 안 고친 것 4개)
- ✅ **4 녹화 12편 완료** — `pipeline/record.py` · `runs/{질문}-{설정}.json` (커밋 `17ff95a` · 실제 ₩356 · 4분)
  - 그물 통과 10 / 탈락 2 · 근거율 0.55~1.00
  - 절제 실험 실측은 **계획서 14장**에. **7장의 기대가 두 군데 어긋났다** — 5장 문구를 여기 맞춰야 한다:
    - 고립 문서 도달이 네 설정 모두 O → 「관계」 질문은 배정 품질을 재지 못한다
    - 역할 끔이 2/3에서 더 나음 → 단, n=1이라 **"끄는 게 낫다"고 쓰면 안 된다**
- ✅ **5 시각화 완성** — 연구소·캐릭터·이벤트 재생 (커밋 `fbfd75d` `0f84e9b`)
  - 소품은 **Furniture Kit 3D 원본을 Blender 로 직접 렌더**했다 (`tools/render_iso.py`).
    기성 아이소메트릭 PNG 는 크롭이 제각각이라 격자에 맞출 기준점이 없다 — 계획서 **15.1**
  - 서고는 **계기 기계 + 게이지 막대**. 가구 모양 바꾸기로는 채움을 표현 못 한다 — **15.3**
  - 재생 12편 12~28초. 큐를 조사관별로 나누고 전역 이벤트만 배리어 — **5.5 · 15.4**
  - **그리기 규칙 셋**(15.2) — 한 축만 늘리지 않기 · 바닥은 먼저 깔기 · 배율을 따로 걸지 않기
- ⏸️ **6 라이브 ← 다음 할 일.** 방문자 본인 키로 실행
- ⏸️ 7 배포

### 이어서 작업할 때 (새 세션·새 PC)

```bash
cd ~/projects/Deep-research-agent
.venv/bin/python -m pipeline.setkey --check     # 키 확인. ❌ 면 아래로 키 재입력
.venv/bin/python -m pipeline.run                # 한 바퀴 (₩40 · 40초)
```

- **venv**: Python 3.12 (`uv venv --python 3.12 .venv` + `uv pip install -r requirements.txt`).
  시스템 기본은 3.14라 라이브러리 호환 문제로 낮춰 잡았다.
- **키**: `.env.local`(권한 600, git 제외)에만 있다. **git 으로 안 따라온다.**
  새 PC·새 클론이면 다시 넣는다 → `python -m pipeline.setkey --from-clipboard`
  (`!` 프리픽스 실행은 tty 가 없어 대화형 입력이 막힌다. 클립보드나 `--from-file` 을 쓴다.)
- **원격**: `github.com/GojoSuperman/Deep-research-agent` (비공개, 2026-09-22 개설). PC 이동 전 push.
- **실행 옵션**: `--no-assign` `--no-zone` `--no-role` `--no-redelegate` `--sections N` `--budget N`
  `--save runs/이름.json` `--quiet`
- **웹앱 보기**: `python3 .superpowers/서버.py web 8732` (백그라운드) → http://127.0.0.1:8732
  캐시를 끄는 서버다. 보통 `http.server` 로 띄우면 브라우저가 옛 모듈을 쥐고 있어 수정이 안 보인다.
- **소품 추가**: `blender -b -P tools/render_iso.py -- <models> <out>` → `python tools/보정.py <out> web/assets/furniture`

## 작업 규칙

- **실측 우선.** 이 프로젝트는 모든 결정을 측정으로 뒷받침해 왔다(후보 3안 코퍼스 비교, 에셋 라이선스 원문 확인). 추측으로 단정하지 말 것.
- **API 키는 어디에도 커밋하지 않는다.** 라이브 모드는 방문자 키를 헤더로만 받고 저장하지 않는다.
- 위키백과 API는 **429 속도 제한**이 있다. 대량 수집 시 요청 간 2~3초 + 백오프 필수.
- 검토용 문서는 marked.js 뷰어 HTML로 렌더해 `python3 -m http.server --bind 127.0.0.1`(백그라운드)로 띄우고 URL 제시.

## 참고

- 시각화 설계 원칙은 `~/projects/agent-office` 에서 **개념만** 가져온다(이벤트 스트림 분리). 코드는 새로 짠다.
- 강의 학습자료: `docs/딥리서치 에이전트 쉽게 이해하기.html` (퀴즈 15개 + 정답)
- 지난 대화 전문: `docs/전체 대화 기록.html`
